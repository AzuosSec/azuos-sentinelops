{
  "name": "azuos-sentinelops",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173 --bind 0.0.0.0 --directory site",
    "build": "mkdir -p dist && cp site/index.html dist/index.html",
    "preview": "python3 -m http.server 4173 --bind 0.0.0.0 --directory dist",
    "dev:host": "python3 -m http.server 4173 --bind 0.0.0.0 --directory site"
  },
  "dependencies": {},
  "devDependencies": {}
}
